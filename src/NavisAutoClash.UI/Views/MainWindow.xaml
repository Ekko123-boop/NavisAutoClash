<Window x:Class="NavisAutoClash.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NavisAutoClash.UI.ViewModels"
        mc:Ignorable="d"
        Title="Auto Clash" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Search -->
            <RowDefinition Height="*"/>    <!-- List -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Select Models for Clash Automation" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>

        <!-- Search Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" Padding="8,5"
                     FontSize="14"/>
            <TextBlock IsHitTestVisible="False" Text="Search models..." 
                       VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Gray"
                       Visibility="{Binding SearchText.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}, ElementName=SearchBox}"/>
        </Grid>

        <!-- Models List -->
        <ListBox Grid.Row="2" ItemsSource="{Binding FilteredModels}" BorderThickness="0" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" Margin="0,0,0,5" Padding="10" CornerRadius="4">
                        <CheckBox IsChecked="{Binding IsSelected}" VerticalContentAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="14" Margin="10,0,0,0"/>
                        </CheckBox>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#666"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Load Models" Command="{Binding LoadModelsCommand}" Padding="15,8" Margin="0,0,10,0"/>
                <Button Content="Run Clash Tests" Command="{Binding RunCommand}" Padding="15,8" Background="#007ACC" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
